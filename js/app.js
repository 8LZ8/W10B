// Referred back to in-class demo and used the examples that were given.
// Created function (selectItem) sets the chosen cookie once it's clicked. 
function selectItem(clickedCookie) {
    Cookies.set("chosenCookie", clickedCookie);
}
